---
title: TODO
description: Planned follow-up work
---

# TODO

- Local workspace sync: `use-workspace-store` and `lib/workspace/workspace-sync` implement the logic but nothing mounts them, so the feature has no UI entry point. Decide whether to add an entry point or remove the dead code.
- Removed-Agent leftovers: the local data model still carries `CanvasAssistantSession`, `chatSessions` / `activeChatId`, `hydrateAssistantImages` and the `agent_chat_messages` storage label from the deleted chat. Clean them up (including export/import) or keep them intentionally.
