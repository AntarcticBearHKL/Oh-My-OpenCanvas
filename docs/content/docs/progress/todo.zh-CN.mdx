---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- 本地工作区同步：`use-workspace-store` 与 `lib/workspace/workspace-sync` 已有逻辑，但没有任何组件挂载，功能没有 UI 入口。需要决定补回入口还是移除死代码。
- 已移除 Agent 的遗留数据：本地数据结构仍保留被删除聊天留下的 `CanvasAssistantSession`、`chatSessions` / `activeChatId`、`hydrateAssistantImages` 与 `agent_chat_messages` 存储标签，需要清理（含导出/导入）或明确保留。
